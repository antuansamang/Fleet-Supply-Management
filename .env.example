# Copia este archivo como .env.local para desarrollo local (no se sube a git).
# En Vercel, estas mismas variables se configuran en Settings -> Environment Variables.

VITE_SUPABASE_URL=https://tu-proyecto.supabase.co
VITE_SUPABASE_ANON_KEY=tu-clave-publicable

# Esta SOLO se configura en Vercel (nunca en un .env que se comparta), la usa /api/claude.js
ANTHROPIC_API_KEY=sk-ant-...
